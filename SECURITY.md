# 安全策略

## 范围

本包运行在用户控制的 DeepSeek Harness 环境中。v0.1 采用本地优先模式：简历、已确认
画像、岗位数据、兴趣台账、报告和生成的网站都属于宿主选定的活跃 Workspace。
插件不得收集、暴露或导出密码、Cookie、Token、浏览器 `localStorage` 凭证或其他认证材料。

## 报告安全问题

请在公开披露前私下向维护者报告安全问题，并附上复现步骤、受影响版本、预期影响和可能的
缓解方案。

仓库公开后，维护者应在 GitHub 仓库设置中启用 **Private vulnerability reporting**；安全问题
请通过仓库的 **Security → Report a vulnerability** 私下提交，不要直接创建公开 Issue。该功能
启用前，请先使用维护者 GitHub 账号的私密联系方式接收报告，并在仓库首页公布安全报告入口。
普通功能问题和非敏感缺陷才使用公开 Issue。

## 运行边界

- BrowserSkill 工具已直接集成，但 CLI 和浏览器扩展由外部软件管理；它们不会打包进本项目，也不会被静默安装。
- BrowserSkill 请求必须只读、仅访问精确主机名白名单、受数量和时间间隔限制，并经过明确批准。
  登录、CAPTCHA、OTP、支付或提交步骤必须停止并请求人工协助，不得绕过。
- 适配器不会提取凭证、提交表单、自动投递岗位或向招聘方发送消息，明确无自动投递行为。
- v0.1 会拒绝扫描版或加密 PDF 简历，不会对其 OCR，也不会上传到其他服务处理。
- Workspace 数据必须留在宿主选定的活跃 Workspace。浏览器 `localStorage` 中的兴趣标记只是
  临时状态，只有用户导出并同步后才会成为正式兴趣台账。
- Windows 快捷方式是写操作，仅以供宿主或手工集成调用的模块级服务形式提供：创建或更新必须
  经过明确批准、有归属 Manifest 且目标网站有效。插件不会自动创建快捷方式；宿主未集成时，
  用户入口尚未提供。
